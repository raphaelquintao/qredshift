name: release.yml
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Release tag to create or update (example: v0.13)"
        required: true
      target_ref:
        description: "Branch or commit to build when running manually"
        required: false
        default: "master"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.target_ref || github.ref }}

      - name: Build Docker image
        run: docker build . -t qredshift-cross

      - name: Build binaries and DEBs
        run: |
          docker run -v $(pwd):/qredshift qredshift-cross sh -c "make x86_64 i686 aarch64 armv7l armv5tel mips64el mipsel powerpc64le s390x debs"
      - name: Create Release
        uses: softprops/action-gh-release@v3.0.1
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && inputs.tag_name || github.ref_name }}
          target_commitish: ${{ github.event_name == 'workflow_dispatch' && inputs.target_ref || github.sha }}
          generate_release_notes: true
          draft: true
          files: |
            build/qredshift_*
            build/*.deb
